services:
  app:
    build:
      context: .
      target: ${APP_BUILD_TARGET:-test}
    ports:
      - "8080:8080"
    environment:
      - Redis__ConnectionString=redis:6379
    depends_on:
      - redis

  mock-football-api:
    build:
      context: .
      target: mock-runtime
    ports:
      - "5081:8080"

  redis:
    image: redis:7-alpine
